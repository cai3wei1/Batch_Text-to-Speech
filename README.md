# Batch_Text-to-Speech
基于python的tts库，使用windows系统自带的语音包，批量将文本转为语音。
Base on the library tts of python, we transform the text to speech with batches using the language bags of windows which have been installed in the factory.
